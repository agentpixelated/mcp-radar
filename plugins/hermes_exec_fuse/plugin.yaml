name: hermes-exec-fuse
version: 0.1.0
description: Batch independent terminal commands, reuse safe results, and prevent redundant tool calls.
author: agentpixelated
provides_tools:
  - exec_fuse
  - exec_fuse_stats
provides_hooks:
  - pre_tool_call
  - post_tool_call
  - pre_llm_call
