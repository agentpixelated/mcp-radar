## Problem

<!-- What problem does this change solve? -->

## Changes

<!-- Keep this focused. List the important behavior or documentation changes. -->

## Security impact

<!-- Explain changes to trust boundaries, secret handling, subprocesses, network access, lockfile compatibility, or risk scoring. Write "None" when not applicable. -->

## Validation

- [ ] `gofmt` is clean
- [ ] `go vet ./...` passes
- [ ] `go test -race ./...` passes
- [ ] `go build ./cmd/mcp-radar` passes
- [ ] documentation is updated when commands, output, or lockfile fields change
- [ ] no secrets, credentials, private paths, or customer data are included

## Compatibility

<!-- Does this change the CLI, output, protocol handling, or lockfile format? If the lockfile format changes, explain the versioning and migration plan. -->

## Screenshots or sample output

<!-- Add sanitized output when the user-facing drift report changes. -->
